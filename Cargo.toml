[package]
name = "lumio-types"

edition = "2021"
version = "0.1.0"

[dependencies]
hex = "0.4.3"
serde = { version = "1.0", features = ["derive"] }
serde_with = "1.9.0"
borsh = { version = "1.2.1", features = ["derive", "unstable__schema"] }

[dev-dependencies]
serde_json = "1.0"
rand = "0.8"
